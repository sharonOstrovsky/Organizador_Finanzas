<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:name=".OrganizadorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/chanchi"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizador"
        tools:targetApi="31">
        <receiver android:name=".ui.views.AlarmNotification"
            android:enabled="true">
        </receiver>
        <activity
            android:name=".ui.views.GraficoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.views.NewExpense"
            android:exported="false" />
        <activity
            android:name=".ui.views.NewTaskSheet"
            android:exported="false" />
        <activity
            android:name=".ui.views.ToDoListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.views.GastosActivity"
            android:exported="false" />
        <activity
            android:name=".ui.views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>